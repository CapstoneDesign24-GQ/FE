import { BsGeoFill } from 'react-icons/bs';

export const Locations={
    header : '지역 선택(도 단위)',
    icon:<BsGeoFill style={{ margin: '0px' }} size={22}/>,
    toggleHeader :'지역 선택',
    elements : ['충청도']
}